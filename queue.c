#include <stdio.h>
#include <stdlib.h>

typedef struct queue {
	short* start;
	int capacity;
	short* eq;
	short* dq;
}


int queue_init( queue *q, int capacity) {
	if (q == 0 ) {

	}
}
int main() {

}